fastapi==0.135.2
uvicorn==0.42.0
fredapi==0.5.2
yfinance==1.2.0
pandas==3.0.1
numpy==2.4.4
scipy==1.17.1
scikit-learn
statsmodels==0.14.6
requests==2.33.0
python-multipart==0.0.20
# COT module: persistence + CFTC fetch packages (§1, §6, §9)
SQLAlchemy==2.0.36
psycopg2-binary==2.9.10          # Postgres driver (prod); SQLite fallback needs no driver
pycot-reports==0.1.2             # primary weekly pull (raw frames via .get_reports)
cftc-cot==0.1.3                  # robust ZIP downloader for the one-time backfill
pydantic-settings==2.6.1         # pycot-reports runtime dependency (not auto-declared)
# beautifulsoup4 is a transitive dep of cftc-cot — left unpinned so pip resolves
# a version with wheels for the active Python (avoids source-build failures).
